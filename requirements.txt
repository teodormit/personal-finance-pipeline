# Core data processing
pandas
numpy

# Excel (.xlsx) support for file-based loads
openpyxl

# Database
psycopg2-binary

# Config / secrets
python-dotenv

# HTTP (BudgetBakers API)
requests

# Orchestration (planned — not yet wired; prototype in flows/expense_pipeline_flow.py)
# prefect>=3.0

# Testing
pytest
pytest-cov

# Optional: Jupyter for exploration
jupyter
ipykernel
