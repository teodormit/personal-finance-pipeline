# Archived scripts

One-off utilities kept for git history. Stale since the BGN→EUR account migration era (early 2026); they duplicate logic now living in `src/` or solved problems specific to that migration.

- `compare_datasets.py` — ad-hoc dataset diff
- `deep_analysis.py` — overlapping dataset diff variant
- `inspect_wallet_export.py` — Wallet export schema inspector
